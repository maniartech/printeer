<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body { font-family: Arial, sans-serif; margin: 1cm; }
        .page { page-break-after: always; padding: 1em; border: 1px solid #ccc; margin-bottom: 1em; }
        .page:last-child { page-break-after: auto; }
        .page-header { background: #f0f8ff; padding: 1em; margin-bottom: 1em; }
        .content { line-height: 1.6; }
    </style>
</head>
<body>
    <% pages.forEach(function(page) { %>
    <div class="page">
        <div class="page-header">
            <h1><%= page.title %></h1>
            <p>Page <%= page.number %> of <%= totalPages %></p>
        </div>
        <div class="content">
            <p><%= page.content %></p>
        </div>
    </div>
    <% }); %>

    <div style="page-break-before: always;">
        <h2>Testing Notes</h2>
        <p>This document contains <%= totalPages %> pages for testing page range selection.</p>
        <p>Test various page range options like: 1-3, 2,4,6, 1-5,8-10, etc.</p>
    </div>
</body>
</html>