<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 700px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .success-container {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%);
            border: 2px solid #28a745;
            border-radius: 8px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .success-icon {
            font-size: 64px;
            color: #28a745;
            margin-bottom: 20px;
        }
        .attempt-summary {
            background: white;
            border-radius: 6px;
            padding: 20px;
            margin: 20px 0;
            text-align: left;
        }
        .success-details {
            background: white;
            border-radius: 6px;
            padding: 20px;
            margin: 20px 0;
            text-align: left;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        .detail-label {
            font-weight: bold;
            color: #495057;
        }
        .detail-value {
            color: #28a745;
            font-family: monospace;
        }
        .retry-timeline {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 15px;
            margin: 20px 0;
        }
        .timeline-item {
            display: flex;
            align-items: center;
            margin: 8px 0;
            padding: 5px 0;
        }
        .timeline-icon {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            margin-right: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            color: white;
            font-weight: bold;
        }
        .timeline-fail {
            background: #dc3545;
        }
        .timeline-success {
            background: #28a745;
        }
        .testing-info {
            background: #e7f3ff;
            border: 1px solid #b3d9ff;
            border-radius: 4px;
            padding: 15px;
            margin: 20px 0;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="success-icon">🎉</div>
        <h1><%= title %></h1>
        <p><%= message %></p>
        
        <div class="attempt-summary">
            <h3>Retry Success Summary</h3>
            <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; text-align: center;">
                <div style="background: #f8d7da; padding: 15px; border-radius: 4px;">
                    <div style="font-size: 24px; font-weight: bold; color: #dc3545;"><%= failedAttempts %></div>
                    <div style="font-size: 12px; color: #721c24;">Failed Attempts</div>
                </div>
                <div style="background: #d4edda; padding: 15px; border-radius: 4px;">
                    <div style="font-size: 24px; font-weight: bold; color: #28a745;"><%= attempt %></div>
                    <div style="font-size: 12px; color: #155724;">Total Attempts</div>
                </div>
                <div style="background: #e7f3ff; padding: 15px; border-radius: 4px;">
                    <div style="font-size: 24px; font-weight: bold; color: #007bff;">100%</div>
                    <div style="font-size: 12px; color: #004085;">Success Rate</div>
                </div>
            </div>
        </div>
        
        <div class="success-details">
            <h3>Success Details</h3>
            <div class="detail-row">
                <span class="detail-label">Final Status:</span>
                <span class="detail-value">200 OK</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Total Attempts:</span>
                <span class="detail-value"><%= attempt %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Failed Attempts:</span>
                <span class="detail-value"><%= failedAttempts %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Success Timestamp:</span>
                <span class="detail-value"><%= new Date().toISOString() %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Counter Status:</span>
                <span class="detail-value">Reset after success</span>
            </div>
        </div>
        
        <div class="retry-timeline">
            <h4>Retry Timeline</h4>
            <% for (let i = 1; i <= attempt; i++) { %>
                <div class="timeline-item">
                    <div class="timeline-icon <%= i <= failedAttempts ? 'timeline-fail' : 'timeline-success' %>">
                        <%= i <= failedAttempts ? '✗' : '✓' %>
                    </div>
                    <span>
                        Attempt <%= i %>: 
                        <%= i <= failedAttempts ? 'Failed' : 'Success' %>
                        <%= i <= failedAttempts ? '(HTTP ' + (500 + (i % 3)) + ')' : '(HTTP 200)' %>
                    </span>
                </div>
            <% } %>
        </div>
        
        <div class="testing-info">
            <h4>✅ Retry Logic Validation</h4>
            <p><strong>This success confirms:</strong></p>
            <ul>
                <li>Retry mechanism is working correctly</li>
                <li>CLI persisted through <%= failedAttempts %> failures</li>
                <li>Error handling allowed continuation</li>
                <li>Final success was properly detected</li>
            </ul>
            
            <p><strong>Next Steps:</strong></p>
            <ul>
                <li>Test with different retry counts</li>
                <li>Verify batch processing continues after retries</li>
                <li>Check error logging and reporting</li>
                <li>Test timeout behavior with retries</li>
            </ul>
            
            <p><strong>Reset for new test:</strong> The counter has been automatically reset. The next request will start failing again from attempt 1.</p>
        </div>
        
        <p style="color: #155724; font-weight: bold; margin-top: 30px;">
            🎯 Retry logic test completed successfully!
        </p>
    </div>
</body>
</html>