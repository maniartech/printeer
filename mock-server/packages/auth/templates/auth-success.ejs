<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .success-container {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%);
            border: 2px solid #28a745;
            border-radius: 8px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .success-icon {
            font-size: 48px;
            color: #28a745;
            margin-bottom: 20px;
        }
        .auth-details {
            background: white;
            border-radius: 6px;
            padding: 20px;
            margin: 20px 0;
            text-align: left;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        .detail-label {
            font-weight: bold;
            color: #495057;
        }
        .detail-value {
            color: #28a745;
            font-family: monospace;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="success-icon">✅</div>
        <h1><%= title %></h1>
        <p>Authentication successful! You have been granted access.</p>
        
        <div class="auth-details">
            <h3>Authentication Details</h3>
            <div class="detail-row">
                <span class="detail-label">Auth Type:</span>
                <span class="detail-value"><%= authType %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Username:</span>
                <span class="detail-value"><%= username %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Credentials:</span>
                <span class="detail-value"><%= credentials %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Timestamp:</span>
                <span class="detail-value"><%= new Date().toISOString() %></span>
            </div>
        </div>
        
        <p><strong>This page confirms that authentication is working correctly for CLI testing.</strong></p>
    </div>
</body>
</html>