<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .login-container {
            background: white;
            border-radius: 8px;
            padding: 40px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border: 1px solid #dee2e6;
        }
        .csrf-info {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 20px;
            color: #856404;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
            color: #495057;
        }
        input[type="text"], input[type="password"], input[type="hidden"] {
            width: 100%;
            padding: 12px;
            border: 2px solid #ced4da;
            border-radius: 4px;
            font-size: 16px;
            box-sizing: border-box;
        }
        input[type="text"]:focus, input[type="password"]:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
        }
        .csrf-token-display {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 10px;
            font-family: monospace;
            font-size: 12px;
            color: #495057;
            margin-top: 5px;
        }
        .submit-btn {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
        }
        .submit-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1><%= title %></h1>
        
        <div class="csrf-info">
            <strong>🛡️ CSRF Protection Enabled</strong><br>
            This form includes CSRF token validation for security testing.
        </div>
        
        <form action="<%= action %>" method="<%= method %>">
            <input type="hidden" name="csrf_token" value="<%= csrfToken %>">
            
            <div class="form-group">
                <label for="username">Username:</label>
                <input type="text" id="username" name="username" required>
                <small style="color: #6c757d;">Use: <code>user</code></small>
            </div>
            
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
                <small style="color: #6c757d;">Use: <code>pass</code></small>
            </div>
            
            <div class="form-group">
                <label for="csrf_display">CSRF Token:</label>
                <div class="csrf-token-display"><%= csrfToken %></div>
                <small style="color: #6c757d;">Token is automatically included in the form</small>
            </div>
            
            <button type="submit" class="submit-btn">Secure Login</button>
        </form>
        
        <p style="margin-top: 20px; text-align: center; color: #6c757d; font-size: 14px;">
            This form demonstrates CSRF protection for secure authentication testing.
        </p>
    </div>
</body>
</html>